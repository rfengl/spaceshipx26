# Normalize line endings to LF in the repository for consistent cross-platform review.
* text=auto eol=lf

# Treat these as binary (no EOL normalization).
*.png binary
*.jpg binary
*.svg text eol=lf
*.pdf binary
